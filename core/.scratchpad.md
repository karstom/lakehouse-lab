<!-- 2026-09-26 14:39 -->

## Phase 4 ANALYST-TRACK (2026-09-26) — candidate nodes for the integrator
- DEC candidate DEC_V3_ANALYST_OWN_SCHEMA_GENERATED_TRINO_RULES: analysts write only lakehouse.dbt_<user>; Trino file rules cannot substitute ${USER} in schema rules, so bootstrap/identity-sync generate rules.json (static config/trino/rules.json + one user-and-schema-scoped rule pair per `analyst` member) into the trino-groups volume; access still only via Keycloak group `analyst`. Tests: tests/bootstrap/test_trino_user_schemas.py (in the analyst cross-cutting patch).
- DEC candidate DEC_V3_SUPERSET_API_BEARER_KEYCLOAK: Superset accepts the user's Keycloak access token (azp jupyterhub only, RS256 via realm JWKS, issuer = LAB_AUTH_URL realm, existing active user only, roles recomputed from token groups) through LabSecurityManager.request_loader (config/superset/lab_bearer.py). Used by A4 checkpoint/reset. Writes still need Superset CSRF token (+ session cookie + Referer).
- DEC candidate: Superset role lab_author (can_write Dataset) for analyst+engineer; Gamma is read-only on datasets so A4 could not create a dataset (HTTP 403 on POST /api/v1/dataset/).
- Gotcha: dbt renders {{ ... }} inside SQL `--` comments (skeleton comment with ref('...') broke compile).
- Gotcha: JupySQL `%%sql duck var <<` silently does nothing (returns None); use duck.sql(...).df() or switch connection first.
- Kernel flake observed once: A4 solve at 14:08:45 UTC in v3-p4-analyst (anna), Superset calls stopped mid-run, kernel never answered, solve subprocess gone; rerun passed in 5.6 s. Evidence ~/lakehouse-v3/p4-analyst-drive2.log.

