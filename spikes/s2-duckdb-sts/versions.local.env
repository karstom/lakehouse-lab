# S-2 extra pins (reported in RESULTS.md). Everything else comes from ../versions.env.
# Base image for the DuckDB client container (python 3.12, matching AIRFLOW_PYTHON / workspace).
PYTHON_IMAGE_TAG=3.12.14-slim-bookworm
