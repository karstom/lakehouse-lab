# Airflow add-ons. pip expands the ${VARS} from the build args (versions.env); no version
# literal lives here. Transitive pins: lock/constraints.txt (GENERATED by relock.sh).
apache-airflow==${AIRFLOW_VERSION}
apache-airflow-providers-keycloak==${AIRFLOW_PROVIDERS_KEYCLOAK_VERSION}
