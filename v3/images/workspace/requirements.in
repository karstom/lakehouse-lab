# Top-level workspace packages. pip expands the ${VARS} from the build args (versions.env);
# no version literal lives here. Transitive pins: lock/constraints.txt (GENERATED by relock.sh).
jupyterhub==${JUPYTERHUB_VERSION}
jupyterlab==${JUPYTERLAB_VERSION}
# Held on 6.x: 7.x can stall a kernel's first shell message (REG_V3_WORKSPACE_KERNEL_FIRST_MESSAGE_STALL).
ipykernel==${IPYKERNEL_VERSION}
jupysql==${JUPYSQL_VERSION}
jupyterlab-git==${JUPYTERLAB_GIT_VERSION}
jupyter-server-proxy==${JUPYTER_SERVER_PROXY_VERSION}
jupyter-ai==${JUPYTER_AI_VERSION}
pyspark-client==${PYSPARK_VERSION}
duckdb==${DUCKDB_VERSION}
pyiceberg[pyarrow,duckdb]==${PYICEBERG_VERSION}
trino==${TRINO_PYTHON_VERSION}
dbt-core==${DBT_CORE_VERSION}
dbt-trino==${DBT_TRINO_VERSION}
