# Trino's built-in TPC-H generator (read-only by nature). Source of the sample data:
# bootstrap/samples.py copies tpch.tiny into lakehouse.samples.* with CREATE TABLE AS.
# Every lab group may read it (rules.json).
connector.name=tpch
